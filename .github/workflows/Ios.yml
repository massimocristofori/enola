- name: Setup Flutter
  uses: subosito/flutter-action@v2
  with:
    channel: stable

- name: Fix iOS platform
  run: |
    flutter create --platforms=ios .
    sed -i '' "s/platform :ios, '.*'/platform :ios, '15.5'/" ios/Podfile

- name: Install pods
  run: |
    cd ios
    pod install
